# ⚽ World Cup 2026 Pool Dashboard

A static dashboard that tracks our World Cup pool. Match scores are pulled
automatically from [football-data.org](https://www.football-data.org/) every
30 minutes by a GitHub Action, and all pool points are computed in the browser.

## Scoring rules

- **Group stage:** 3 pts per win, 1 pt per draw, **+5** for finishing first in the group.
- **Knockouts:** **+5** for each round reached — Round of 32, Round of 16,
  quarter-final, semi-final, final. (The third-place match doesn't score; a
  bonus for winning the final can be enabled via `championBonus` in
  `data/pool.json`, currently 0.)
- **Player score** = sum of their 3 teams. Ties broken by combined goal
  difference, then combined goals scored.
- **🥄 Wooden spoon:** the single worst team — fewest group-stage points, then
  worst goal difference, then most goals against. Awarded to its owner.

## One-time setup

1. **Get a free API token** at
   [football-data.org/client/register](https://www.football-data.org/client/register)
   (the free tier includes the FIFA World Cup).
2. **Add it as a repo secret:** Settings → Secrets and variables → Actions →
   New repository secret, name it `FOOTBALL_DATA_TOKEN`.
3. **Enable GitHub Pages:** Settings → Pages → Source: *Deploy from a branch* →
   branch `main`, folder `/ (root)`. The dashboard will be at
   `https://<user>.github.io/wc-pool-2026/`.
4. (Optional) Run the **Update scores** workflow manually from the Actions tab
   to pull the first batch of data instead of waiting for the schedule.

## Preview with fake data

Open the dashboard with `?demo=1` appended to the URL to see a fully simulated
tournament (random groups/results — generated by `scripts/make_demo.py`).

Locally:

```bash
python3 -m http.server          # then open http://localhost:8000/?demo=1
```

## Manual corrections

`data/overrides.json` lets you fix anything without touching code:

- `groupWinners`: force a group winner (e.g. `{"A": "Mexico"}`) if a group is
  decided by tiebreakers beyond points/GD/goals-for (head-to-head is attempted
  automatically; fair play or drawing of lots is not).
- `extraMatches`: add match objects the API is missing (same shape as entries
  in `data/matches.json`).
- `excludeMatchIds`: ignore bad matches by id.

The dashboard shows a yellow warning banner whenever it needs a manual call.

## Project layout

| Path | Purpose |
| --- | --- |
| `index.html`, `assets/` | The dashboard (static, no build step) |
| `assets/scoring.js` | All pool-scoring logic (pure module) |
| `data/pool.json` | Players, teams, scoring config, team-name aliases |
| `data/matches.json` | Normalized match results (written by the fetcher) |
| `data/overrides.json` | Manual corrections |
| `scripts/fetch_scores.py` | Pulls scores from football-data.org |
| `scripts/test_scoring.mjs` | Tests — run `node scripts/test_scoring.mjs` |
| `.github/workflows/update-scores.yml` | 30-minute score refresh |

## After the tournament

Disable the **Update scores** workflow from the Actions tab (or delete the
cron block) so it stops polling.
